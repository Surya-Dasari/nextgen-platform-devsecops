apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: nextgen
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: suryadasari31/frontend:1.0.26-e4dd27d-SNAPSHOT
          imagePullPolicy: Always
          ports:
            - containerPort: 9080

