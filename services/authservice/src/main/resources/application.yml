server:
  port: ${SERVER_PORT:9001}

userservice:
  url: ${USERSERVICE_URL:http://localhost:9002}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always

