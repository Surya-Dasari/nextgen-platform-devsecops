apiVersion: apps/v1
kind: Deployment
metadata:
  name: authservice
  namespace: nextgen
spec:
  replicas: 1
  selector:
    matchLabels:
      app: authservice
  template:
    metadata:
      labels:
        app: authservice
    spec:
      containers:
        - name: authservice
          image: authservice:ubi
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9001
          envFrom:
            - configMapRef:
                name: authservice-config
            - secretRef:
                name: authservice-secret

