apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nextgen-ingress
  namespace: dev
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nextgen-ui
                port:
                  number: 9080

          - path: /api
            pathType: Prefix
            backend:
              service:
                name: apiservice
                port:
                  number: 9000
